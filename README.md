# csv-profile

A lightweight, zero-dependency Python command-line utility built to rapidly profile and assess the basic data health of tabular CSV/TSV data records straight from the terminal.

## Usage

Install the command-line utility in your environment using `uv`:
```bash
uv add "git+[https://github.com/](https://github.com/amruthapotluri/csv-profile)
